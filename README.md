# Vue 3 + Vite

This template should help get you started developing with **Vue 3** in **Vite**.  
The template uses Vue 3 `<script setup>` SFCs — check out the [script setup docs](https://vuejs.org/api/sfc-script-setup.html) to learn more.

Learn more about IDE Support for Vue in the [Vue Docs Scaling Up Guide](https://vuejs.org/guide/scaling-up/tooling.html).

---

## 🚀 Getting Started

### 1️⃣ Install dependencies
```bash
npm install
2️⃣ Run the project (development)
bash
Copy code
npm run dev
Then open the local URL displayed in your terminal (usually http://localhost:5173).

🏗️ Build for production
bash
Copy code
npm run build
The production-ready files will be generated inside the dist/ folder.

🔍 Preview the production build
bash
Copy code
npm run preview
🧩 Common npm commands
Command	Description
npm install	Install dependencies
npm run dev	Run development server
npm run build	Build the project for production
npm run preview	Preview the production build

🪄 Notes
Make sure you have Node.js ≥ 18 installed.

The folders node_modules and dist should be ignored in Git (via .gitignore):

nginx
Copy code
node_modules
dist
